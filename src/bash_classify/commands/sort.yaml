# $schema: ../../../schemas/command.schema.json
command: sort
description: "Sort lines of text files"
classification: READONLY
strict: false
global_options:
  --help: {overrides: READONLY}
options:
  -o: {takes_value: true, overrides: LOCAL_EFFECTS, names_output_path: true}
  --output: {takes_value: true, overrides: LOCAL_EFFECTS, aliases: [-o], names_output_path: true}
