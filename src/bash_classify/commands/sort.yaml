# $schema: ../../../schemas/command.schema.json
command: sort
description: "Sort lines of text files"
classification: READONLY
strict: false
options:
  -o: {takes_value: true, overrides: LOCAL_EFFECTS}
  --output: {takes_value: true, overrides: LOCAL_EFFECTS, aliases: [-o]}
