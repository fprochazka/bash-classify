# $schema: ../../../schemas/command.schema.json
command: mv
description: "Move or rename files and directories"
classification: LOCAL_EFFECTS
strict: false
global_options:
  --help: {overrides: READONLY}
options:
  -t: {takes_value: true, names_output_path: true}
  --target-directory: {takes_value: true, aliases: [-t], names_output_path: true}
